#include "shell.h"

/**
 * main - Entry point of the shell program
 *
 * Return: Always 0
 */

int main(void)
{
	/**
	 * The main entry point calls the method defined in shell.c on start
	 */
	shell_loop();
	return (0);
}
